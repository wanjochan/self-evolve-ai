/**
 * callchain_test.c - 测试loader→runtime→program调用链
 */

#include <stdio.h>

int main() {
    printf("Callchain test: loader→runtime→program working!\n");
    printf("This program was executed through the complete call chain.\n");
    return 0;
}
