# 简单的TASM测试程序

.section .text
    # 入口点
    entry_point:
        push rbp
        mov rbp, rsp
        call func
        pop rbp
        ret
        
    # 函数
    func:
        push rbp
        mov rbp, rsp
        xor rax, rax
        pop rbp
        ret 